# Commentaar op de website-variant (AIDEC)

Gegenereerd op basis van broncode-analyse — 2026-03-23

---

## Algemeen

- Strak donker ontwerp (`#0D0D0D`) met blauwe accent (`#4B9FFF`). Consistent en professioneel.
- Scrollanimaties (reveal, fadeIn, slideUp) zijn consequent toegepast.
- Alle secties hebben dezelfde basisstructuur: scheidingslijn → overline label → grote heading → body.

---

## Problemen / aandachtspunten

### 1. Gebroken logo in de navigatiebalk

`Navigation.tsx` importeert `aidec-logo-opt.png`, maar het bestand in `src/assets/` heet `aidec-logo.png` (zonder `-opt`).
→ Het logo in de navigatiebalk is waarschijnlijk een broken image.

### 2. Taalinconsistentie in de Hero

De hoofdtitel luidt: **"Where intelligence meets engineering."** — dit is Engels.
Alle andere tekst op de pagina is Nederlands.
→ Ofwel de volledige site in het Engels zetten, ofwel de titel vertalen.

### 3. Dubbele navigatielink

In de nav wijzen zowel **"Collectief"** als **"Contact"** naar `#contact`.
→ Eén van de twee is overbodig. "Collectief" is ook een vage/onduidelijke benaming voor bezoekers.

### 4. Secties About en Questions ontbreken in de nav

De navigatiebalk bevat links naar: Collectief, The Grid, Deelnemen, Contact.
De secties `#about` (01 / WAAROM AIDEC) en `#questions` (02 / CENTRALE VRAGEN) zijn **niet bereikbaar** via de nav.
→ Voeg deze toe of hernoem bestaande links.

### 5. "The Grid" is Engels

De navigatielink heet **"The Grid"** terwijl de rest van de site Nederlands is.
→ Aanpassen naar bijv. "Het Raster" of "AI per fase", of consequent overschakelen naar Engels.

### 6. Imec-logo mist een label

In de Contact-sectie hebben UAntwerpen ("Productontwikkeling") en UGent ("Industrieel Ontwerpen") elk een beschrijving naast hun logo.
De Imec-kaart heeft **geen tekst** naast het logo.
→ Voeg een label toe zoals "Technologie & AI" of laat alle drie de labels weg voor consistentie.

### 7. Projectperiode ligt in de toekomst

De prijstabel vermeldt: *"01/10/2026 t/m 30/09/2028"* met de noot *"Onder voorbehoud van goedkeuring"*.
→ Dit is bewust zo, maar controleer of deze tekst zichtbaar genoeg is voor bezoekers die zich al willen inschrijven.

---

## Verbeterpunten (optioneel)

- **Imec ontbreekt in de Hero.** De partnerlogo's onderaan de Hero tonen enkel UAntwerpen en UGent. Imec is pas zichtbaar in de Contact-sectie. Overweeg Imec ook in de Hero toe te voegen.
- **Navigatievolgorde vs. sectievolg orde.** De navigatie loopt: Collectief → The Grid → Deelnemen → Contact. De pagina zelf loopt: Hero → About → Questions → Mapping → Participation → Contact. Dit klopt niet overeen. Bezoekers die de nav volgen scrollen niet in de logische volgorde.
- **Prijstabelanimatie.** De rijen in de prijstabel gebruiken een hardcoded `opacity: 0` + `animation: fadeIn forwards` zonder scroll-trigger. Dit werkt enkel correct als de tabel al zichtbaar is bij het laden van de pagina. Bij traag scrollen kan de animatie al afgelopen zijn voordat de bezoeker de tabel bereikt.

---

## Design Review (Design Review Agent)

### Schoonheid

#### Kleurgebruik

Het palet is strikt en gedisciplineerd: bijna-zwart `#0D0D0D` als achtergrond, één accentkleur `#4B9FFF` en een gelaagde witte transparantieschaal (`white/8`, `white/15`, `white/25`, `white/35`, `white/40`, `white/50`). Dit is sterk en professioneel — het vermijdt kleurchaos en geeft de site een duidelijk karakter.

Probleem: de accentkleur `#4B9FFF` is een vrij generieke "tech-blauw" tint die overal in SaaS-design opduikt. Ze is functioneel, maar mist persoonlijkheid. Er is geen warme tint, geen subtiele complementaire kleur, geen gradientspel in de tekst of UI-elementen zelf. De site is kleurarm op een manier die saaiheid riskeer in plaats van raffinement. Vergelijk met sites als Linear.app of Vercel waar subtiele blauwe en paarse gradients extra diepte geven.

De `bg-white/3`, `bg-white/4`, `bg-white/6` achtergronden voor kaarten zijn zo subtiel dat ze op slechte schermen vrijwel onzichtbaar zijn. `bg-[#111]` is concreter en werkt beter, maar de inconsistentie (sommige kaarten `#111`, andere `white/4`) is een compositorische zwakte.

#### Typografie

Inter als enige font is een veilige keuze. De groottes zijn ambitieus en goed: `text-7xl font-black` met `leading-[1.05] tracking-tight` in de Hero en About-koppen is krachtig en eigentijds. De staggered heading reveal (twee regels, één wit, één `white/30`) in About is het mooiste typografische moment van de site.

Zwakke punten:

- De bodytekst staat consequent op `text-white/40` of zelfs `white/35`. Dit geeft een contrast ratio die voor veel gebruikers de WCAG AA-drempel niet haalt. De informatie is letterlijk moeilijk leesbaar gemaakt omwille van esthetiek.
- Er is geen tweede fontkeuze of variatie. De `font-mono` klassen voor labels en nummers (`text-xs font-mono tracking-widest`) zijn een goede touch — het scheidt metadata van inhoud. Maar een secundair display-font voor headings zou de visuele rijkdom aanzienlijk verhogen.
- De sectiescheiding via `text-xs font-mono text-[#4B9FFF] tracking-widest` labels ("— 01 / WAAROM AIDEC") is consistent en elegant. Dit patroon werkt.

#### Witruimte

`py-40` (160px) per sectie is genereus en geeft de site ruimte om te ademen — dit is correct voor een premiumpositionering. De interne marges (`mb-20`, `mb-16`, `mb-12`) zijn consequent. Geen klachten hier; dit is een van de sterkere aspecten van de site.

#### Compositie en visuele hiërarchie

De hiërarchie is consistent: overline label → grote kop → bodytekst → content grid. Dit werkt. De bento-grid in Mapping met variabele kolombreedtes (`lg:col-span-7`, `lg:col-span-5`, `lg:col-span-4`) is de meest interessante compositie van de site, hoewel het pas zichtbaar is op desktop.

Zwak punt: de **Questions-sectie** (02 / CENTRALE VRAGEN) is de armste sectie compositorisch. Vier tekstvakken in een 2×2 grid met een nummerlabel en een vraagzin — geen icon, geen visueel accent, geen afwisseling in grootte. Dit voelt als opvulling. De kaarten hebben nauwelijks onderscheidende kenmerken van elkaar en de sectie draagt niet bij aan de visuele reis.

De **Contact-sectie** is uit verhouding: een 2-kolom grid dat begrensd is op `max-w-4xl`, terwijl alle andere secties `max-w-7xl` gebruiken. Dit creëert een onverwacht smal, bijna krap gevoel aan het einde van de pagina — juist daar waar je de gebruiker wil overtuigen.

Het **partner logo-blok** in Hero (`bg-white/6 border border-white/10 px-5 py-3 rounded`) is functioneel maar visueel lelijk: twee rechthoekige witte dozen met logo's naast elkaar, zonder enige sfeer. Dit is het eerste ding wat potentiële partners zien over zichzelf.

---

### Modernheid

#### Animaties

De animatiestrategie is coherent en technisch goed uitgevoerd:

- Hero: staggered `slideUp` voor headings + `fadeIn` voor onderliggende elementen, met cubic-bezier `0.16, 1, 0.3, 1` (een spring-achtige ease) — dit is modern en voelt polished.
- Scroll reveal: `.reveal`, `.reveal-stagger`, `.reveal-line`, `.reveal-heading` zijn alle goed gedefinieerd in `globals.css`. De stagger-delays van 80ms per kind zijn subtiel genoeg om niet irritant te worden.
- De pulserende glow-orbs in Hero (`animate-glow-pulse`, `animate-glow-pulse-slow`) zijn een bekende tech-site trope maar werken hier redelijk: ze geven diepte zonder afleidend te zijn.

Zwak punt: na de Hero-sectie treedt er een animatiedaling op. De `card-hover` klasse (`translateY(-4px)` bij hover) is een minimale, brave interactie. Er is geen hover-state die tekst laat groeien, geen reveal van verborgen inhoud, geen kleurshift van de achtergrond bij hover op de kaarten zelf. De micro-interacties stoppen bij de navigatie-knop ("Neem deel") en de ArrowRight-icoon shift (`group-hover:translate-x-1`) — correct maar alomtegenwoordig in 2024-2025 design.

De custom cursor (`.cursor-dot` + `.cursor-ring`) is een welkome ambitie, maar het is een tweekoppige implementatie: de ring reageert op hover via `.is-hovering` maar de JavaScript die dit aanstuurt is niet zichtbaar in de geanalyseerde bestanden. Als dit correct geïmplementeerd is, voegt het echte karakter toe. Als het half werkt of laggy is, ondermijnt het de hele indruk.

Het film grain overlay (`.grain-overlay` met `opacity: 0.04`) is subtiel en goed — het geeft de bijna-zwarte achtergrond een organische textuur die anders digitaal-kaal aanvoelt. Dit is een teken van designbewustzijn.

#### Layoutkeuzes

De full-viewport Hero met centered content is standaard maar correct voor een "wij zijn er" introductie. Het scroll-indicator element (het miniatuur muisicoon) is charmant maar de animatie is `animate-bounce` — de standaard Tailwind bounce, die een stuiterende verticale beweging geeft die stijlbreukt met de subtiele spring-easing die overal elders gebruikt wordt.

De bento-grid in Mapping is de meest moderne layoutkeuze op de site. Echter: op mobile (`grid-cols-1`) vervalt elke asymmetrie en ziet het er uit als een gewone gestapelde lijst. Er is geen mobile-specifieke compositie overwogen.

De prijstabel in Participation is een van de weinige plaatsen waar echte data gepresenteerd wordt. Een plain HTML `<table>` met subtiele borders is functioneel maar biedt een kans voor iets interessanters: een vergelijkingskaart-layout of een highlight op de meest voorkomende categorie (bijv. MO) zou de conversie versterken.

#### Trends vs. tijdloos

De site speelt bewust in op de "dark tech dashboard" esthetiek (Linear, Vercel, Raycast-stijl). Dit is een coherente keuze maar ook het meest gespeelde speelveld van 2022-2025. Het is op dit moment tijdloos genoeg om niet als gedateerd over te komen, maar ook niet onderscheidend genoeg om memorabel te zijn. Er is geen eigenheid die zegt "dit is AIDEC", behalve het logo. De combinatie van `#0D0D0D` + blauw accent + mono-labels + fade-up reveals is identiek aan honderden andere sites in de AI/tech-ruimte.

---

### Top 5 Prioritaire Verbeterpunten

1. **Contrast en leesbaarheid van bodytekst verhogen.**
  `text-white/35` en `text-white/40` op donkere achtergronden is esthetisch gewenst maar accessibility-problematisch. Verhoog naar minimaal `white/55` voor beschrijvende tekst in kaarten (bijv. `p className="text-sm text-white/35"` in `About.tsx` regel 63 en `Mapping.tsx` regel 90). De informatie die VLAIO-partners moeten lezen is nu letterlijk moeilijker leesbaar gemaakt omwille van esthetiek — dit is geen afweging die je wil maken.
2. **Questions-sectie redesignen — nu de zwakste sectie van de site.**
  De vier vragen in een 2x2 grid zonder visuele differentiatie (`Questions.tsx`) ogen als een placeholder. Overweeg: één grote vraag per sectie als statement-typografie, een horizontale scroll-sectie, of interactieve accordion-expand per vraag. Nu is er geen reden om stil te staan bij deze content.
3. **Contact-sectie uitbreiden naar volledige breedte en versterken.**
  De `max-w-4xl`-beperking in `Contact.tsx` (regel 20) en de schaarsheid van de contactinformatie (één naam, één e-mailadres in een kleine kaart) ondermijnen de closing power van de pagina. Dit is het laatste wat een beslisser ziet. Voeg een duidelijkere CTA-sectie toe boven de partners, of vergroot de contactkaart significant.
4. **Accentkleur meer karakter geven via gradient of second accent.**
  `#4B9FFF` is de enige kleur met karakter op de site. Overweeg een subtiele gradient in prominente headings (bijv. `linear-gradient(135deg, #4B9FFF, #A78BFA)`) of een tweede accent (paars of cyaan) voor specifieke highlights. Dit geeft AIDEC een eigenheid die het nu mist ten opzichte van generieke tech-sites. Minimale impact op codebase, maximale impact op merkbeleving.
5. `**animate-bounce` vervangen en card hover-states verrijken.**
  In `Hero.tsx` (regel 99) gebruikt de scroll-indicator `animate-bounce` — de enige plek op de site die Tailwind's standaard bounce gebruikt, wat stijlbreukt met de verfijnde `cubic-bezier(0.16, 1, 0.3, 1)` easing overal elders. Vervang door een custom `@keyframes` animation die dezelfde spring-ease hanteert. Breid daarnaast de `card-hover` interactie uit: de huidige `translateY(-4px)` + lichtere border is correct maar minimaal. Een subtiele `background-color` shift of een accentkleur-gloed op het icon bij hover zou de interactielaag significant verrijken.

# Design Feedback — AIDEC Website

*Gegenereerd door: Design Review Agent*

---

## Schoonheid

### Kleurgebruik

Het palet is strikt en gedisciplineerd: bijna-zwart `#0D0D0D` als achtergrond, één accentkleur `#4B9FFF` en een gelaagde witte transparantieschaal (`white/8`, `white/15`, `white/25`, `white/35`, `white/40`, `white/50`). Dit is sterk en professioneel — het vermijdt kleurchaos en geeft de site een duidelijk karakter.

Probleem: de accentkleur `#4B9FFF` is een vrij generieke "tech-blauw" tint die overal in SaaS-design opduikt. Ze is functioneel, maar mist persoonlijkheid. Er is geen warme tint, geen subtiele complementaire kleur, geen gradientspel in de tekst of UI-elementen zelf. De site is kleurarm op een manier die saaiheid riskeer in plaats van raffinement. Vergelijk met sites als Linear.app of Vercel waar subtiele blauwe en paarse gradients extra diepte geven.

De `bg-white/3`, `bg-white/4`, `bg-white/6` achtergronden voor kaarten zijn zo subtiel dat ze op slechte schermen vrijwel onzichtbaar zijn. `bg-[#111]` is concreter en werkt beter, maar de inconsistentie (sommige kaarten `#111`, andere `white/4`) is een compositorische zwakte.

### Typografie

Inter als enige font is een veilige keuze. De groottes zijn ambitieus en goed: `text-7xl font-black` met `leading-[1.05] tracking-tight` in de Hero en About-koppen is krachtig en eigentijds. De staggered heading reveal (twee regels, één wit, één `white/30`) in About is het mooiste typografische moment van de site.

Zwakke punten:

- De bodytekst staat consequent op `text-white/40` of zelfs `white/35`. Dit geeft een contrast ratio die voor veel gebruikers de WCAG AA-drempel niet haalt. De informatie is letterlijk moeilijk leesbaar gemaakt omwille van esthetiek.
- Er is geen tweede fontkeuze of variatie. De `font-mono` klassen voor labels en nummers (`text-xs font-mono tracking-widest`) zijn een goede touch — het scheidt metadata van inhoud. Maar een secundair display-font voor headings zou de visuele rijkdom aanzienlijk verhogen.
- De sectiescheiding via `text-xs font-mono text-[#4B9FFF] tracking-widest` labels ("— 01 / WAAROM AIDEC") is consistent en elegant. Dit patroon werkt.

### Witruimte

`py-40` (160px) per sectie is genereus en geeft de site ruimte om te ademen — dit is correct voor een premiumpositionering. De interne marges (`mb-20`, `mb-16`, `mb-12`) zijn consequent. Geen klachten hier; dit is een van de sterkere aspecten van de site.

### Compositie en visuele hiërarchie

De hiërarchie is consistent: overline label → grote kop → bodytekst → content grid. Dit werkt. De bento-grid in Mapping met variabele kolombreedtes (`lg:col-span-7`, `lg:col-span-5`, `lg:col-span-4`) is de meest interessante compositie van de site, hoewel het pas zichtbaar is op desktop.

Zwak punt: de **Questions-sectie** (02 / CENTRALE VRAGEN) is de armste sectie compositorisch. Vier tekstvakken in een 2×2 grid met een nummerlabel en een vraagzin — geen icon, geen visueel accent, geen afwisseling in grootte. Dit voelt als opvulling. De kaarten hebben nauwelijks onderscheidende kenmerken van elkaar en de sectie draagt niet bij aan de visuele reis.

De **Contact-sectie** is uit verhouding: een 2-kolom grid dat begrensd is op `max-w-4xl`, terwijl alle andere secties `max-w-7xl` gebruiken. Dit creëert een onverwacht smal, bijna krap gevoel aan het einde van de pagina — juist daar waar je de gebruiker wil overtuigen.

Het **partner logo-blok** in Hero (`bg-white/6 border border-white/10 px-5 py-3 rounded`) is functioneel maar visueel lelijk: twee rechthoekige witte dozen met logo's naast elkaar, zonder enige sfeer. Dit is het eerste ding wat potentiële partners zien over zichzelf.

---

## Modernheid

### Animaties

De animatiestrategie is coherent en technisch goed uitgevoerd:

- Hero: staggered `slideUp` voor headings + `fadeIn` voor onderliggende elementen, met cubic-bezier `0.16, 1, 0.3, 1` (een spring-achtige ease) — dit is modern en voelt polished.
- Scroll reveal: `.reveal`, `.reveal-stagger`, `.reveal-line`, `.reveal-heading` zijn alle goed gedefinieerd in `globals.css`. De stagger-delays van 80ms per kind zijn subtiel genoeg om niet irritant te worden.
- De pulserende glow-orbs in Hero (`animate-glow-pulse`, `animate-glow-pulse-slow`) zijn een bekende tech-site trope maar werken hier redelijk: ze geven diepte zonder afleidend te zijn.

Zwak punt: na de Hero-sectie treedt er een animatiedaling op. De `card-hover` klasse (`translateY(-4px)` bij hover) is een minimale, brave interactie. Er is geen hover-state die tekst laat groeien, geen reveal van verborgen inhoud, geen kleurshift van de achtergrond bij hover op de kaarten zelf. De micro-interacties stoppen bij de navigatie-knop ("Neem deel") en de ArrowRight-icoon shift (`group-hover:translate-x-1`) — correct maar alomtegenwoordig in 2024-2025 design.

De custom cursor (`.cursor-dot` + `.cursor-ring`) is een welkome ambitie, maar het is een tweekoppige implementatie: de ring reageert op hover via `.is-hovering` maar de JavaScript die dit aanstuurt is niet zichtbaar in de geanalyseerde bestanden. Als dit correct geïmplementeerd is, voegt het echte karakter toe. Als het half werkt of laggy is, ondermijnt het de hele indruk.

Het film grain overlay (`.grain-overlay` met `opacity: 0.04`) is subtiel en goed — het geeft de bijna-zwarte achtergrond een organische textuur die anders digitaal-kaal aanvoelt. Dit is een teken van designbewustzijn.

### Layoutkeuzes

De full-viewport Hero met centered content is standaard maar correct voor een "wij zijn er" introductie. Het scroll-indicator element (het miniatuur muisicoon) is charmant maar de animatie is `animate-bounce` — de standaard Tailwind bounce, die een stuiterende verticale beweging geeft die stijlbreukt met de subtiele spring-easing die overal elders gebruikt wordt.

De bento-grid in Mapping is de meest moderne layoutkeuze op de site. Echter: op mobile (`grid-cols-1`) vervalt elke asymmetrie en ziet het er uit als een gewone gestapelde lijst. Er is geen mobile-specifieke compositie overwogen.

De prijstabel in Participation is een van de weinige plaatsen waar echte data gepresenteerd wordt. Een plain HTML `<table>` met subtiele borders is functioneel maar biedt een kans voor iets interessanters: een vergelijkingskaart-layout of een highlight op de meest voorkomende categorie (bijv. MO) zou de conversie versterken.

### Trends vs. tijdloos

De site speelt bewust in op de "dark tech dashboard" esthetiek (Linear, Vercel, Raycast-stijl). Dit is een coherente keuze maar ook het meest gespeelde speelveld van 2022-2025. Het is op dit moment tijdloos genoeg om niet als gedateerd over te komen, maar ook niet onderscheidend genoeg om memorabel te zijn. Er is geen eigenheid die zegt "dit is AIDEC", behalve het logo. De combinatie van `#0D0D0D` + blauw accent + mono-labels + fade-up reveals is identiek aan honderden andere sites in de AI/tech-ruimte.

---

## Top 5 Prioritaire Verbeterpunten

1. **Contrast en leesbaarheid van bodytekst verhogen.**
  `text-white/35` en `text-white/40` op donkere achtergronden is esthetisch gewenst maar accessibility-problematisch. Verhoog naar minimaal `white/55` voor beschrijvende tekst in kaarten (bijv. `p className="text-sm text-white/35"` in `About.tsx` regel 63 en `Mapping.tsx` regel 90). De informatie die VLAIO-partners moeten lezen is nu letterlijk moeilijker leesbaar gemaakt omwille van esthetiek — dit is geen afweging die je wil maken.
2. **Questions-sectie redesignen — nu de zwakste sectie van de site.**
  De vier vragen in een 2x2 grid zonder visuele differentiatie (`Questions.tsx`) ogen als een placeholder. Overweeg: één grote vraag per sectie als statement-typografie, een horizontale scroll-sectie, of interactieve accordion-expand per vraag. Nu is er geen reden om stil te staan bij deze content.
3. **Contact-sectie uitbreiden naar volledige breedte en versterken.**
  De `max-w-4xl`-beperking in `Contact.tsx` (regel 20) en de schaarsheid van de contactinformatie (één naam, één e-mailadres in een kleine kaart) ondermijnen de closing power van de pagina. Dit is het laatste wat een beslisser ziet. Voeg een duidelijkere CTA-sectie toe boven de partners, of vergroot de contactkaart significant.
4. **Accentkleur meer karakter geven via gradient of second accent.**
  `#4B9FFF` is de enige kleur met karakter op de site. Overweeg een subtiele gradient in prominente headings (bijv. `linear-gradient(135deg, #4B9FFF, #A78BFA)`) of een tweede accent (paars of cyaan) voor specifieke highlights. Dit geeft AIDEC een eigenheid die het nu mist ten opzichte van generieke tech-sites. Minimale impact op codebase, maximale impact op merkbeleving.
5. `animate-bounce` **vervangen en card hover-states verrijken.**
  In `Hero.tsx` (regel 99) gebruikt de scroll-indicator `animate-bounce` — de enige plek op de site die Tailwind's standaard bounce gebruikt, wat stijlbreukt met de verfijnde `cubic-bezier(0.16, 1, 0.3, 1)` easing overal elders. Vervang door een custom `@keyframes` animation die dezelfde spring-ease hanteert. Breid daarnaast de `card-hover` interactie uit: de huidige `translateY(-4px)` + lichtere border is correct maar minimaal. Een subtiele `background-color` shift of een accentkleur-gloed op het icon bij hover zou de interactielaag significant verrijken.

---

## Technische Review — Performance & Toegankelijkheid Agent

*Geanalyseerd op: bronbestanden + [http://localhost:3001/website/](http://localhost:3001/website/)*

### Toegankelijkheid (a11y)

**Contrast ratios — meerdere overtredingen**

De volgende kleurcombinaties halen de WCAG 2.1 AA-drempel (4.5:1 voor normale tekst, 3:1 voor grote tekst) niet:

- `text-white/35` (`rgba(255,255,255,0.35)`) op `#0D0D0D`: contrast ratio ≈ 2.0:1. Gebruikt in `About.tsx` regel 63 (`text-white/35`), `Mapping.tsx` regel 90 (`text-white/35`), `Participation.tsx` regel 64 (`text-white/35`).
- `text-white/40` op `#0D0D0D`: contrast ratio ≈ 2.3:1. Gebruikt in body copy in `Hero.tsx` (regel 52) en `About.tsx` (regel 50) — dit is de *primaire* beschrijvingstekst van het project, die het hardst leesbaar moet zijn.
- `text-white/25` op `#0D0D0D` (sectielabels, tabelkoppen): contrast ratio ≈ 1.45:1. Volledig onleesbaar voor mensen met visuele beperkingen.
- `text-white/20` (`Participation.tsx` regel 74, tabelvoetnoot): contrast ratio ≈ 1.15:1. Functioneel onzichtbaar.
- Nav-links `text-white/50` op `#0D0D0D/90`: ≈ 3.0:1 — haalt AA voor grote tekst maar niet voor 14px-tekst.

**Ontbrekende semantische structuur**

- `Hero.tsx`: De twee heading-regels "Where intelligence meets engineering." zijn `<div>`-elementen (regels 38–46), geen `<h1>`. De pagina heeft daardoor geen H1, wat essentieel is voor screenreaders en SEO.
- `About.tsx` regels 39–46: De manifesto-koppen ("Wij gaan verder dan de hype." / "Wij bouwen de brug.") zijn `<span>` elementen binnen `<div class="reveal-heading">`, niet `<h2>`. Zelfde patroon in `Participation.tsx` regel 28 en `Contact.tsx` regel 15.
- `Questions.tsx` regel 23: De vraagzinnen zijn `<p>`-elementen zonder koppeling aan de nummerlabels. Voor screenreaders zijn de `01`/`02` labels losse tekst zonder relatie.
- `Participation.tsx` tabel (regels 79–101): De `<table>` heeft geen `<caption>` of `aria-label`. Screenreaders kondigen de tabel aan zonder context.

**Focus management**

- `cursor: none` is globaal ingesteld op `body, a, button` via `@media (pointer: fine)` (`globals.css` regels 287–290). Dit verbergt de native focus-ring voor keyboard-gebruikers op desktop — een pointer-device sluit keyboard-gebruik niet uit. Er is nergens een custom `:focus-visible` stijl gedefinieerd als vervanging. Keyboard-navigatie is hierdoor visueel blind.
- Het mobiele menu (`Navigation.tsx` regels 60–86) gebruikt `max-h-0 opacity-0` voor verbergen maar geen `aria-expanded`, `aria-controls` of `hidden`-attribuut. De menu-knop (regel 46) heeft wel `aria-label="Menu"` maar communiceert de open/dicht-staat niet aan assistive technology.
- De `useReveal`-hook (`useReveal.ts`) voegt `is-visible` toe maar verwijdert het nooit. Elementen beginnen met `opacity: 0` — als JavaScript laad-/uitvoerfouten optreden, blijft content *permanent onzichtbaar*. Er is geen `<noscript>` fallback.

**Overige a11y-problemen**

- `Navigation.tsx` regel 9: twee navItems hebben `href: '#contact'` (`Collectief` en `Contact`). Dit leidt tot dubbele anchor-links die screenreaders verwarren.
- `Contact.tsx` regel 52: de mailto-link toont het e-mailadres als linktekst — dit is correct. Maar er is geen `aria-label` op de `<Mail>`-icon die de visuele decoratie van de tekst scheidt.
- Geen `lang`-attribuut op sectieniveau; de pagina mixt Engels (Hero heading) en Nederlands zonder `lang`-aanduiding op de Engelse fragmenten.

---

### Performance

**Afbeeldingsformaten en -groottes**

- `logo_UGent_NL_RGB_2400_kleur_witbg.png`: dit bestand is ~1.7MB (een 2400px-brede PNG-export uit de UGent huisstijl). Het wordt gerenderd op `h-8` (32px hoog) met `object-contain`. Effectief rendert de browser een afbeelding van >2400×900px op ~80×32px. Dit is een factor ~500 overkill. Geen `width`/`height`-attribuut op de `<img>`, wat Cumulative Layout Shift (CLS) veroorzaakt. Gebruikt in zowel `Hero.tsx` (regel 86–90) als `Contact.tsx` (regels 30–36) — dubbele lading van hetzelfde grote bestand.
- De Figma-assets (`1cb2ac71f09b11e719653dac260438a910c5bd79.png`, `ccb6f66f4bcef309f545c7e1cd0253900f6ea7f4.png`) zijn ook PNG zonder gespecificeerde dimensies. Geen van de `<img>`-elementen op de site heeft `width` en `height`-attributen.
- Geen enkel logo-asset is geconverteerd naar WebP of AVIF. De build (`vite.config.ts`) heeft geen `assetsInlineLimit` of imagemin-plugin geconfigureerd.

**requestAnimationFrame loop — onbeperkt actief**

- `App.tsx` regels 46–53: de `animate()`-functie wordt permanent via `requestAnimationFrame` uitgevoerd, ook wanneer de muis stilstaat of de pagina niet zichtbaar is (`document.hidden === true`). Er is geen `document.addEventListener('visibilitychange', ...)` guard. Op een typische 60Hz-scherm zijn dit 60 DOM-schrijfoperaties per seconde (`ringEl.style.left`, `ringEl.style.top`) continu, ongeacht activiteit. Dit verhindert dat de browser de tab in een energiebesparende staat plaatst.
- De ring-animatie schrijft rechtstreeks naar `style.left` en `style.top` — dit triggert `layout` (reflow) per frame. Gebruik `transform: translate(x, y)` in plaats van `left`/`top` voor GPU-compositing zonder reflow.

**CSS-animaties**

- `globals.css` regels 335–351: `animate-glow-pulse` en `animate-glow-pulse-slow` animeren `opacity` én `transform: scale()`. Dit is correct (compositor-only). Geen probleem.
- `globals.css` regels 256–264: `.card-hover:hover` schrijft `transform: translateY(-4px)`. Dit is compositor-only. Correct.
- `.cursor-ring` CSS (`globals.css` regels 312–315) heeft `transition: top 0.1s ease-out, left 0.1s ease-out` — maar `top`/`left` worden al via JavaScript én via CSS-transition geanimeerd. Dit is een dubbele animatielaag die ongewenst gedrag kan geven (de CSS-transition conflicteert met de RAF-loop). De `transition` op `top`/`left` kan worden verwijderd; de JS-lerp doet al de smoothing.

**Bundle-grootte**

- `package.json` bevat de volledige shadcn/radix-ui componentenset (accordion, alert-dialog, avatar, checkbox, collapsible, context-menu, dialog, dropdown-menu, hover-card, label, menubar, navigation-menu, popover, progress, radio-group, scroll-area, select, separator, slider, switch, tabs, toggle, toggle-group, tooltip) plus recharts, embla-carousel, react-day-picker, react-hook-form, vaul, cmdk, sonner, next-themes, input-otp. Van geen van deze wordt er ook maar één component daadwerkelijk gebruikt in de geanalyseerde bronbestanden. Dit zijn pure dead dependencies die via Vite's tree-shaking mogelijk niet worden weggeschreven, maar de `node_modules`-install is ~300-500MB zwaarder dan nodig en CI/deploy-tijden lopen op.
- `vite.config.ts` heeft geen `rollupOptions.output.manualChunks` gedefinieerd. De volledige app wordt als één bundle geserveerd.

**Font-laden**

- `index.html` laadt Inter via Google Fonts met `display=swap`. Er is geen `<link rel="preload">` voor het font-bestand zelf, wat een FOUT (Flash of Unstyled Text) veroorzaakt bij trage verbindingen. Inter wordt geladen in gewichten 400/600/800/900 — het gewicht 700 ontbreekt terwijl `font-semibold` (600) en `font-bold` (700) beide worden gebruikt; de browser zal 700 synthetiseren vanuit 800, wat subtiele rendering-artefacten geeft.

---

### Technische correctheid

**IntersectionObserver in useReveal.ts**

- `useReveal.ts` regels 5–19: de observer wordt aangemaakt in `useEffect` zonder dependency array — dit is correct (eenmalig). De `return () => observer.disconnect()` cleanup is aanwezig.
- **Kritiek probleem**: `querySelectorAll` wordt aangeroepen op `document` op het moment van mount. In een React SPA is dit het moment *nadat* de initiële render klaar is — dit werkt voor statische content. Maar als componenten later gemount worden (bijv. via lazy loading of conditionele render), worden hun `.reveal`-elementen *nooit geobserveerd*. De hook heeft geen `MutationObserver` of herstart-mechanisme.
- Elementen worden nooit `unobserve()`d na het triggeren van `is-visible`. Dit is een bewuste keuze (eenmalige reveal), maar de observer blijft alle elementen monitoren ook na de transitie. Voeg `observer.unobserve(entry.target)` toe na `classList.add('is-visible')` om geheugengebruik te beperken bij lange pagina's.
- `threshold: 0.12` en `rootMargin: '0px 0px -40px 0px'` zijn redelijke waarden. De negatieve bottom-margin voorkomt vroege trigger — correct.

**CSS-animaties op** `<tr>`**-elementen in Participation.tsx**

- `Participation.tsx` regels 92–93: tabelrijen krijgen inline `style={{ opacity: 0, animation: 'fadeIn 0.5s ease ${index * 80}ms forwards' }}`. Deze animaties starten *altijd* bij mount, ongeacht of de tabel in beeld is. Ze zijn niet gekoppeld aan de scroll-reveal. De tabel is deep in de pagina — de animatie is allang voorbij voordat een gebruiker er naartoe scrollt. Dit is een logica-bug: gebruik de reveal-class of initieer de animatie vanuit een IntersectionObserver.

**Navigatie-anker conflict**

- `Navigation.tsx` regels 6–9: zowel `Collectief` als `Contact` linken naar `#contact`. Er is geen `#collectief`-sectie. `Contact.tsx` definieert `id="contact"`. Dit is een bug: de "Collectief"-link navigeert naar de Contact-sectie in plaats van een eigen sectie.

`figma:asset/` **imports**

- Meerdere bestanden importeren via `figma:asset/...` protocol (`Hero.tsx` regel 3, `Contact.tsx` regels 2 en 4). Dit is een Figma-Dev-Mode-specifiek import-protocol, opgelost via aliases in `vite.config.ts`. In een standaard build-omgeving zonder deze config zou dit crashes veroorzaken. De alias-opzet werkt lokaal maar is fragiel voor CI/CD-pipelines die de Vite-config niet kennen.

`!important` **in .card-hover**

- `globals.css` regel 259: `.card-hover` gebruikt `!important` op `border-color` en `background-color` transitions. Dit overschrijft alle Tailwind-utilities op deze properties voor card-elementen — een onderhoudsrisico. In `About.tsx` wordt `card-hover` gecombineerd met `bg-[#0D0D0D]`; de `!important` voorkomt dat Tailwind's `hover:bg-`* utilities ooit werken op deze kaarten.

---

### Mobiele ervaring

**cursor: none op touch-devices — gedeeltelijk opgelost, maar incompleet**

- `globals.css` regels 287–290: `cursor: none` is correct omsloten in `@media (pointer: fine)`. Touch-screens gebruiken `(pointer: coarse)` en zien de cursor-override niet.
- `App.tsx` regel 37: de JavaScript-check `window.matchMedia('(pointer: fine)').matches` is aanwezig en voorkomt dat de RAF-loop start op touch-devices. Dit is correct.
- **Residu-probleem**: de `.cursor-dot` en `.cursor-ring` DOM-elementen worden altijd gerenderd (`App.tsx` regels 86–87), ook op touch-devices. Ze staan op `position: fixed; top: 0; left: 0` en beginnen in de linkerbovenhoek. Op touch-devices zijn ze niet zichtbaar (6px wit bolletje op donkere achtergrond, potentieel buiten viewport), maar ze bezetten z-index 99998/99999 en kunnen theoretisch touch-events blokkeren op elementen in die zone. De elementen zouden conditioneel niet gerenderd moeten worden op `(pointer: coarse)`.

**Responsive layout-problemen**

- `Mapping.tsx` regels 77–103: het bento-grid gebruikt `grid-cols-1` op mobile. De `lg:col-span-7` / `lg:col-span-5` / `lg:col-span-4` sizing is alleen actief vanaf `lg` (1024px). Op tablet (768px–1023px) vallen alle items terug op `grid-cols-1` als een gestapelde lijst — de `md`-breakpoint is niet gedefinieerd voor dit grid. Overweeg `md:grid-cols-2` als tussenformat.
- `Contact.tsx` regel 20: `grid md:grid-cols-2` — op mobile is dit een single-column layout. Acceptabel, maar de `gap-16` (64px) wordt ook op mobile toegepast en is excessief voor een small viewport.
- `Hero.tsx` regels 9–20: de glow-orbs zijn `w-[600px] h-[600px]` en `w-[400px] h-[400px]` als vaste pixels. Op een 390px-brede iPhone zijn deze orbs breder dan het viewport, wat horizontale overflow kan veroorzaken als `overflow-hidden` op de `<section>` ontbreekt of per ongeluk wordt overreden. De sectie heeft wel `overflow-hidden` (regel 7), dus dit is afgedekt — maar het is een breekbaar patroon.

`py-40` **op alle secties**

- Elke sectie gebruikt `py-40` (160px padding boven en onder). Op mobile is 160px verticale padding per sectie excessief — sectieinhoud staat ver van het viewport-centrum. Standaardpraktijk is `py-20 sm:py-32 lg:py-40` met een responsive ladder. Dit is puur een UX-kwaliteitsaanmerking zonder a11y-impact.

**Scroll progress bar**

- `App.tsx` regels 16–23: de scroll-listener heeft `{ passive: true }` — correct voor performance. De `setProgress` React state-update triggert een re-render op elke scroll-event. Op mobile met snel scrollen zijn dit tientallen re-renders per seconde. Gebruik `useRef` + directe DOM-manipulatie op de progressbar (`style.width`) om React-re-renders te vermijden, zoals al gedaan is voor de cursor-elementen.

---

### Must-fix issues (gerankt op ernst)

1. **[Kritiek — a11y] Geen H1 op de pagina.** `Hero.tsx` regels 38–46 gebruiken `<div>`-elementen voor de primaire heading. Elke pagina moet precies één `<h1>` hebben. Screenreaders en zoekmachines vinden geen headingstructuur. Fix: wrap de eerste heading-div in een `<h1>`.
2. **[Kritiek — a11y]** `cursor: none` **zonder** `:focus-visible` **vervanging.** `globals.css` regels 287–290 verbergen de native keyboard-cursor op alle pointer-fine-devices. Keyboard-gebruikers kunnen niet zien welk element de focus heeft. Fix: voeg een expliciete `:focus-visible` stijl toe (bijv. `outline: 2px solid #4B9FFF; outline-offset: 3px`) vóór de `@media (pointer: fine)` block, of gebruik `:focus-visible` i.p.v. `:focus` zodat de native ring enkel voor mouse-interactions verdwijnt.
3. **[Hoog — performance] UGent-logo 1.7MB PNG.** `src/assets/logo_UGent_NL_RGB_2400_kleur_witbg.png` wordt dubbel geladen (Hero + Contact) op `h-8` (32px). Exporteer een versie op max. 160×64px als WebP, onder 5KB. Voeg `width` en `height`-attributen toe aan alle `<img>`-tags om CLS te elimineren.
4. **[Hoog — performance] RAF-loop actief zonder visibility-check.** `App.tsx` regels 46–53: de cursor-animatieloop runt permanent, ook als de tab niet zichtbaar is. Voeg `document.addEventListener('visibilitychange', ...)` toe om de loop te pauzeren bij `document.hidden === true`. Migreer ook van `style.left`/`style.top` naar `style.transform` om layout-reflows te elimineren.
5. **[Hoog — a11y] Meerdere WCAG-contrastfouten in bodytekst.** `text-white/35`, `text-white/40` en `text-white/25` op `#0D0D0D` halen geen 4.5:1. De slechtste gevallen (`white/25`, `white/20`) halen zelfs 3:1 niet voor grote tekst. Minimumfix: verhoog bodytekst naar `white/60`, sectielabels naar `white/45`.
6. **[Hoog — technisch] Tabelrij-animaties starten bij mount, niet bij scroll.** `Participation.tsx` regels 92–93: `animation: 'fadeIn 0.5s ease ${index * 80}ms forwards'` is al voorbij als de gebruiker de sectie bereikt. Koppel aan de scroll-reveal door de animatie via `.is-visible` op de parent te triggeren i.p.v. via inline style.
7. **[Midden — a11y] Mobiel menu zonder aria-expanded/hidden.** `Navigation.tsx` regel 46–63: de menu-toggle heeft `aria-label="Menu"` maar geen `aria-expanded={isOpen}` en geen `aria-controls`. Het uitgeklapte menu heeft geen `role="navigation"` of `aria-label`. Voeg beide toe.
8. **[Midden — technisch]** `.reveal`**-elementen die niet in beeld zijn geweest blijven permanent** `opacity: 0` **als JS faalt.** Er is geen CSS-fallback. Overweeg een `<noscript>`-block of een `@media (prefers-reduced-motion: no-preference)` guard die `.reveal` pas de startstate geeft als de gebruiker animaties accepteert, en de elementen anders direct zichtbaar maakt.
9. **[Laag — performance] Dead dependencies in package.json.** Volledige Radix UI componentenset, recharts, embla-carousel, react-day-picker en andere packages zijn geïnstalleerd maar niet gebruikt. Verwijder uit `package.json` voor kleinere `node_modules` en snellere installs. Tree-shaking in Vite voorkomt dat ze in de bundel terechtkomen, maar de install-overhead blijft.
10. **[Laag — technisch] CSS-transition conflict op** `.cursor-ring`**.** `globals.css` regels 312–315 definiëren `transition: top 0.1s, left 0.1s` op het ring-element, maar de JS RAF-loop overschrijft `left`/`top` elk frame. De CSS-transition heeft geen effect en voegt onnodige style-berekening toe. Verwijder de `top`/`left` uit de transition-definitie.

---

## Review op basis van draaiende site — Visual Design Agent

*URL: [http://localhost:3001/website/](http://localhost:3001/website/)*

### Schoonheid

**Kleurenpalet**  
Het palet is strak en goed gedisciplineerd: `#0D0D0D` als bijna-zwarte basis, één accentkleur `#4B9FFF` (CSS-variabele `--primary` in `globals.css` r.11), en een gelaagde witte transparantieschaal. De lagen `white/8 → white/15 → white/25 → white/35 → white/40 → white/50` worden consequent als diepte-indicatoren gebruikt. Sterk.

Probleem 1 — inconsistente kaartachtergronden: `bg-white/3` en `bg-white/4` (gebruikt in `Contact.tsx` r.25, r.29 en `About.tsx` r.70) zijn op gamma-gecalibreerde schermen vrijwel identiek aan `#0D0D0D`. `bg-[#111]` (gebruikt in `About.tsx` r.60 en `Participation.tsx` r.47 en `Mapping.tsx` r.83) is concreter en visueel betrouwbaarder. De twee benaderingen worden door elkaar gebruikt voor visueel identieke kaartcomponenten — dit is compositorisch inconsistent.

Probleem 2 — hardcoded hex vs. CSS-variabelen: het `.dark`-blok in `globals.css` r.44–79 overschrijft `--primary` naar vrijwel wit en `--accent` naar donkergrijs. Maar alle CTA-knoppen (`Hero.tsx` r.68, `Navigation.tsx` r.39, `Participation.tsx` r.108, `Contact.tsx` r.54) gebruiken hardcoded `bg-[#4B9FFF]` in plaats van `bg-primary`. De CSS-variabelen zijn dus decoratief en worden door de componenten genegeerd. Dit is een architectuurinconsistentie die theming onmogelijk maakt.

**Typografie**  
Inter `font-black` op `text-5xl` tot `text-7xl` met `leading-[1.05] tracking-tight` is krachtig en eigentijds. Het sterkste typografische moment: `About.tsx` r.38–47 — twee heading-regels, één op volledig wit, één op `white/30`, met staggered reveal. Dit is het enige compositorische moment dat echt onderscheidend aanvoelt.

De `font-mono tracking-widest` sectielabels ("— 01 / WAAROM AIDEC") zijn een elegante metadata-laag die inhoud van navigatie scheidt. Dit patroon is consequent doorgevoerd in alle secties.

Zwakte: de basis-typografische stijlen in `globals.css` r.138–182 (h1–h4, label, button, input met `font-weight-medium: 500`) zijn vrijwel dode code. Alle componenten gebruiken Tailwind-utility-klassen die deze regels overschrijven. De stylesheet-architectuur heeft een overbodig laagje.

**Witruimte**  
`py-40` (160px) per sectie is genereus en correct voor een premium positionering. De sectionele cadans — `reveal-line` → overline → heading → body → grid — is in elke sectie identiek. Dit geeft rust en voorspelbaarheid. Het is tegelijkertijd ook het enige ritme: er is geen sectie die bewust breekt met het patroon om accent te geven.

**Visuele hiërarchie**  
De hiërarchie werkt op sectieniveau. Op kaartniveau is er een contrast-probleem: beschrijvende tekst staat op `text-white/35` (`About.tsx` r.63, `Mapping.tsx` r.90, `Participation.tsx` r.64) — dat is ca. `#585858` op `#0D0D0D`, een contrast ratio van ±3.4:1. WCAG AA vereist 4.5:1 voor normale tekst. De inhoud die VLAIO-partners moeten lezen is bewust moeilijker leesbaar gemaakt ten gunste van esthetiek.

De **Questions-sectie** (`Questions.tsx`) is compositorisch de zwakste: vier identieke kaarten in een 2×2 grid, elk met een monokleur nummerlabel en één vraagzin. Geen icon, geen variatie in grootte, geen visueel accent. De sectie oogt als een placeholder die nog gevuld moet worden.

De **Contact-sectie** (`Contact.tsx` r.20: `max-w-4xl`) is smaller dan alle andere secties (`max-w-7xl`). Dit geeft een ongewild krap gevoel aan het einde van de pagina — het moment waarop je een beslisser maximaal wil overtuigen.

---

### Modernheid & Interactie

**Animatiesysteem — sterk fundament**  
De animatie-architectuur is coherent en goed geïsoleerd. Vier CSS-klassen in `globals.css` r.199–253 (`.reveal`, `.reveal-stagger`, `.reveal-line`, `.reveal-heading`) dekken alle scenario's. De `cubic-bezier(0.16, 1, 0.3, 1)` spring-ease is consequent overal — in mount-animaties (`Hero.tsx` r.37, 43), scroll-reveals, card-hover (`globals.css` r.257) en nav-underline (`globals.css` r.280). Dit geeft het systeem een eenduidige fysieke taal.

De Hero-headings gebruiken `overflow: hidden` op `.reveal-heading` (r.243–245) zodat de `translateY(110%)` startpositie niet zichtbaar is. Dit is correct: tekst die opkomt vanuit een clipping-container oogt als printerletter-inkt die droogt — een premium effect.

**Kritische stijlbreuk:** `animate-bounce` **op scroll-indicator**  
`Hero.tsx` r.99: de scroll-indicator dot gebruikt `animate-bounce` — Tailwind's ingebouwde bounce-keyframe die een discontinue, harde stuitering produceert. Dit is de enige plek op de site die deze easing gebruikt, in directe visuele nabijheid van de meest verfijnd geanimeerde sectie. Dit is de meest zichtbare stijlbreuk in het volledige animatiesysteem.

**Custom cursor — technisch solide, risicovol zonder fallback**  
`App.tsx` r.26–72: de implementatie is correct — `requestAnimationFrame` voor ring-lerp (factor 0.1), directe DOM-manipulatie voor dot, gedelegeerde hover-detectie. De `@media (pointer: fine)` CSS-guard (`globals.css` r.287) en de `window.matchMedia` JS-check (`App.tsx` r.37) zijn beide aanwezig. Echter: als het JavaScript faalt of te langzaam laadt, is `cursor: none` al ingesteld via CSS en heeft de gebruiker geen cursor. Er is geen CSS-fallback die `cursor: auto` herstelt als `.cursor-dot` niet in de DOM staat.

**Micro-interacties — te minimaal na de Hero**  
`.card-hover:hover` in `globals.css` r.261–264 geeft `translateY(-4px)` en een lichtere border. Dat is het. Er is geen achtergrondkleurshift, geen icon-kleurchange op de kaart, geen tekstintensivering. De hover-state van de Mapping-cards (`Mapping.tsx` r.87: `group-hover:text-[#4B9FFF]` op het icon) is de meest rijke hover-response op de site — en die is beperkt tot een kleurwisseling van één pictogram. `.card-hover` in `globals.css` r.259 heeft `!important` op de transition, wat het onmogelijk maakt om vanuit Tailwind-utilities een `hover:bg-`* toe te voegen op kaarten — dit blokkadeert actief micro-interactie-uitbreiding.

**Film grain overlay**  
`.grain-overlay` op `opacity: 0.04` is op de grens van perceptie maar geeft de `#0D0D0D` achtergrond een organische textuur. De statische SVG `feTurbulence` animeert niet, er is dus geen CPU-last. Dit is een subtiel maar bewust designmoment.

**Scroll progress bar**  
`App.tsx` r.14–23: correcte implementatie met `{ passive: true }` scroll-listener. De `setProgress` state-update triggert echter een React re-render bij elke scroll-event — op mobile bij snel scrollen zijn dit tientallen re-renders per seconde. De cursor-elementen zijn al correct geïmplementeerd met directe DOM-manipulatie (`App.tsx` r.40–42); dezelfde aanpak zou hier de re-renders elimineren.

---

### UX & Flow

**Paginastructuur en narratieve volgorde**  
De volgorde in `App.tsx` r.89–95: Hero → About → Questions → Participation → Mapping → Contact. Het logische probleem: Questions ("Wat zijn de centrale vragen?") staat na About ("Waarom AIDEC?") — maar de vragen zijn de motivatie voor het bestaan van AIDEC, niet een gevolg ervan. En Mapping ("The Grid") staat na Participation ("Doe mee") — een bezoeker wordt gevraagd deel te nemen voordat ze de inhoudelijke waarde van het project zien. De meest overtuigende volgorde zou zijn: Hero → About → Mapping → Questions → Participation → Contact.

**Navigatielinkbug**  
`Navigation.tsx` r.5–10: `Collectief` en `Contact` verwijzen beiden naar `href="#contact"`. Dit is een bug: een gebruiker die op "Collectief" klikt navigeert naar de Contact-sectie. Er is geen `#collectief` sectie-id op de pagina. Bovendien ontbreekt een link naar `#about` in de navigatie — de tweede sectie is via de nav onbereikbaar. De nav heeft vier items waarvan er twee identiek zijn en één ontbreekt.

**CTA-consistentie**  
Drie identieke CTA-knoppen linken naar het Microsoft Forms-formulier (`Hero.tsx` r.64, `Navigation.tsx` r.36, `Participation.tsx` r.104) met verschillende labels. De variatie is correct: contextuele framing per plek. De primaire CTA in de Hero (`px-8 py-4`) is visueel helder en dominant genoeg. De nav-CTA (`px-5 py-2`) is kleiner maar herkenbaar. Goed.

**Leesbaarheid van sleuteltekst**  
De subheading in de Hero (`Hero.tsx` r.51: `text-white/40`) is de eerste uitleg van wat AIDEC doet — en staat op de slechtst leesbare opacity van de bodytekst. Dit is de tekst die een onbekende bezoeker moet overtuigen. `text-white/40` op `#0D0D0D` = contrast ratio ±3.9:1. Haalt AA niet voor 18px normale tekst.

**Pricing tabel als conversiemoment**  
`Participation.tsx` r.71–113: de bijdragetabel toont vijf bedrijfstypen met bijdragen van €500 tot €5.000. Er is geen visuele highlight op de meest voorkomende categorie. De "Starter"-rij (€500) en "VZW"-rij (€1.500 in kind) zijn gelijkwaardig getoond aan de €5.000-rij voor grote ondernemingen. Een highlight (accentborder, achtergrondkleur, badge) op de `MO`-rij zou de leesbaarheid en conversiefocus sterk verbeteren.

---

### Performance & Toegankelijkheid

**UGent logo — 1.7MB voor een 32px-hoge afbeelding**  
`src/assets/logo_UGent_NL_RGB_2400_kleur_witbg.png` is een 2400px brede PNG die tweemaal geladen wordt (`Hero.tsx` r.86, `Contact.tsx` r.31) en weergegeven op `h-8` (32px) / `h-10` (40px) via `object-contain`. De effectieve laadgrootte is ±3.4MB voor wat 5KB SVG of 15KB WebP zou kunnen zijn. Geen `width`/`height`-attributen op de `<img>`, dus ook Cumulative Layout Shift. Dit is het grootste single performance-probleem op de site.

`figma:asset/` **imports — productie-onklaar**  
`Hero.tsx` r.3 en `Contact.tsx` r.3–4 importeren via `figma:asset/...` protocol. Dit werkt uitsluitend in de Figma Dev Mode preview-omgeving via een Vite-alias. In een standaard productie-build of CI-pipeline zonder die alias zijn de UA-logo en het imec-logo onzichtbaar. Dit is een blocker voor elke echte deploy.

**Contrast ratios — concrete WCAG-metingen**

- `text-white/15` (`Contact.tsx` r.65, footer): ≈1.5:1 — volledig buiten alle WCAG-normen.
- `text-white/20` (`Hero.tsx` r.79, partner-label): ≈1.9:1 — niet-compliant.
- `text-white/25` (sectielabels in `About.tsx` r.71, `Participation.tsx` r.40): ≈2.4:1 — haalt 3:1 niet voor grote tekst.
- `text-white/35` (kaartbeschrijvingen in `About.tsx` r.63, `Mapping.tsx` r.90): ≈3.4:1 — haalt 4.5:1 niet voor `text-sm`.
- `text-white/40` (bodytekst `Hero.tsx` r.52, `About.tsx` r.50): ≈3.9:1 — haalt 4.5:1 niet.

`prefers-reduced-motion` **volledig afwezig**  
Nergens in `globals.css` of `useReveal.ts` wordt `prefers-reduced-motion` gerespecteerd. Alle entrance-animaties, glow-pulse loops en reveal-transitions worden afgespeeld voor gebruikers die in hun OS animaties hebben uitgeschakeld. Dit is een toegankelijkheidsovertreding voor gebruikers met vestibulaire stoornissen.

**RAF-loop zonder visibility-guard**  
`App.tsx` r.46–53: de cursor-animatieloop runt ononderbroken, ook als de browser-tab niet zichtbaar is. Er is geen `document.visibilitychange`-check. Additioneel schrijft de loop naar `style.left` en `style.top` per frame — dit triggert layout reflow. Gebruik `style.transform` voor compositor-only updates.

**Geen H1 op de pagina**  
`Hero.tsx` r.34–47: de primaire heading "Where intelligence meets engineering." is een `<div>`, geen `<h1>`. De pagina heeft geen semantische heading-structuur. Dit raakt zowel screenreader-gebruikers als zoekmachine-indexering.

**Keyboard-focus volledig onzichtbaar**  
`globals.css` r.287–290: `cursor: none` op `body, a, button` verbergt ook de native focus-indicator voor keyboard-gebruikers. Er is nergens een `:focus-visible` stijl gedefinieerd. Keyboard-navigatie op de site is visueel blind.

---

### Must-fix (gerankt op impact)

1. `figma:asset/` **imports vervangen door lokale** `@/assets/` **paden** — `Hero.tsx` r.3 en `Contact.tsx` r.3–4. De site is onklaar voor productie-deploy zolang logo's via het Figma-specifieke protocol geladen worden. Dit is een deploy-blocker.
2. **UGent logo downscalen naar WebP/SVG** — `src/assets/logo_UGent_NL_RGB_2400_kleur_witbg.png` laadt 1.7MB (dubbel = 3.4MB) voor een 32–40px hoge weergave. Exporteer als SVG of WebP op max. 160px breed. Voeg `width` en `height` toe aan alle `<img>`-tags om CLS te elimineren. Impact: grootste single performance-winst op de site.
3. **Contrast bodytekst verhogen** — `text-white/35` en `text-white/40` falen WCAG AA. Minimale fix: kaartbeschrijvingen naar `white/55`, primaire bodytekst naar `white/60`, sectielabels van `white/25` naar `white/40`. De informatie die een potentiële VLAIO-partner moet lezen is nu structureel moeilijker leesbaar gemaakt omwille van esthetiek.
4. **H1 toevoegen en semantische heading-hiërarchie herstellen** — `Hero.tsx` r.38: eerste heading-div omzetten naar `<h1>`. Sectiehoofdingen in `About.tsx`, `Participation.tsx`, `Contact.tsx` omzetten van `<span>` naar `<h2>`. Kaarthoofdingetjes in `About.tsx` r.62 en `Participation.tsx` r.63 zijn al correct `<h4>`.
5. `:focus-visible` **stijl definiëren voor keyboard-navigatie** — `globals.css`: voeg vóór de `@media (pointer: fine)` block een `:focus-visible` regel toe (bijv. `outline: 2px solid #4B9FFF; outline-offset: 3px`) zodat keyboard-gebruikers visuele feedback hebben, onafhankelijk van de custom cursor.
6. `prefers-reduced-motion` **media query toevoegen** — `globals.css`: voeg een `@media (prefers-reduced-motion: reduce)` block toe dat de keyframe-animaties (`slideUp`, `fadeIn`, `glow-pulse`, `glow-pulse-slow`) en alle `.reveal`-transitions uitschakelt en elementen direct zichtbaar maakt.
7. **Navigatielinkbug oplossen** — `Navigation.tsx` r.6: `Collectief` wijst naar `#contact`. Ofwel verwijder het item, ofwel geef de partners-subsectie in `Contact.tsx` een eigen `id="collectief"`. Voeg ook een `#about` link toe — de tweede sectie is momenteel via de nav onbereikbaar.
8. **Tabelrij-animaties koppelen aan scroll-reveal** — `Participation.tsx` r.92–93: de inline `fadeIn`-animaties starten bij mount, niet bij scroll. De animatie is allang voorbij als de gebruiker de sectie bereikt. Vervang door de `.reveal-stagger` klasse op de `<tbody>` of de tabelrijen.



